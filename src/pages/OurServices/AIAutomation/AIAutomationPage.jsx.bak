import { useState, useEffect, useRef } from "react";
import { Link } from "react-router-dom";
import { AnimatePresence, motion, useInView, useScroll, useTransform } from "framer-motion";
import {
  ArrowRight, CheckCircle, Clock, Users, Award, Shield, Zap, Star,
  ChevronDown, Loader2, FileText, BookOpen, PenTool, Sparkles,
  Send, Play, Pause, FileCheck, FileSpreadsheet, FileImage, FileCode,
  Download, Upload, RefreshCw, Table, List, AlignLeft,
  Type, Hash, Eye, EyeOff, Save, Printer, Layout,
  Columns, Rows, Grid, PanelTop, PanelBottom, Sidebar, FileUp,
  Folder, CheckSquare, Square, ThumbsUp, MessageSquare, UserCheck,
  FolderOpen, ExternalLink, Globe, GitBranch, Lightbulb, BarChart3,
  Search, Scan, Highlighter, Eraser, Check, X, PenLine, MousePointer,
  BookMarked, GraduationCap, Target, TrendingUp, Layers, ArrowRightCircle,
  SpellCheck, Pencil, AlignJustify, Library, Quote, Clock4, Hourglass,
  Rocket, Eye as EyeIcon, Filter, BookA, ScrollText, User,
  Bot, Brain, Cpu, Workflow, GitMerge, BarChart, LineChart, PieChart,
  Activity, Database, Cloud, Server, Code, Terminal, Settings, Sliders
} from "lucide-react";
import Navbar from "../../../components/Navbar/Navbar";
import Footer from "../../../components/Footer/Footer";
import styles from "./AIAutomationPage.module.css";
import SEO from "../../../components/SEO/SEO";
import { apiService } from "../../../services/api";

// ========== TYPEWRITER HOOK ==========
const ROTATING_WORDS = [
  "Workflow Automation",
  "AI Agents",
  "Intelligent Analytics",
  "Predictive Insights",
  "Autonomous Decision-Making"
];

function useTypewriter(words, speed = 100, pause = 2400) {
  const [text, setText] = useState("");
  const [wordIndex, setWordIndex] = useState(0);
  const [deleting, setDeleting] = useState(false);
  const [holding, setHolding] = useState(false);

  useEffect(() => {
    const current = words[wordIndex % words.length];
    let t;
    if (!deleting && text.length < current.length) {
      t = setTimeout(() => setText(current.slice(0, text.length + 1)), speed);
    } else if (!deleting && text.length === current.length) {
      setHolding(true);
      t = setTimeout(() => { setHolding(false); setDeleting(true); }, pause);
    } else if (deleting && text.length > 0) {
      t = setTimeout(() => setText(current.slice(0, text.length - 1)), speed / 2);
    } else {
      setDeleting(false);
      setWordIndex(i => i + 1);
    }
    return () => clearTimeout(t);
  }, [text, deleting, wordIndex, words, speed, pause]);

  return { text, holding };
}

// ========== ANIMATED STAT COUNTER ==========
function AnimatedStat({ value, suffix = "" }) {
  const ref = useRef(null);
  const inView = useInView(ref, { once: true, amount: 0.6 });
  const [display, setDisplay] = useState("0");
  const target = parseFloat(value.replace(/,/g, ""));
  const hasDecimal = value.includes(".");

  useEffect(() => {
    if (!inView || isNaN(target)) {
      setDisplay(value);
      return;
    }
    let frame;
    const duration = 1400;
    const start = performance.now();
    const tick = (now) => {
      const progress = Math.min((now - start) / duration, 1);
      const eased = 1 - Math.pow(1 - progress, 3);
      const current = target * eased;
      const formatted = hasDecimal
        ? current.toFixed(1)
        : Math.round(current).toLocaleString();
      setDisplay(formatted + suffix);
      if (progress < 1) frame = requestAnimationFrame(tick);
    };
    frame = requestAnimationFrame(tick);
    return () => cancelAnimationFrame(frame);
  }, [inView, target, value, suffix, hasDecimal]);

  return <span ref={ref}>{display}</span>;
}

// ========== ANIMATION VARIANTS ==========
const fadeUp = {
  hidden: { opacity: 0, y: 28 },
  visible: { opacity: 1, y: 0 }
};

const scaleIn = {
  hidden: { opacity: 0, scale: 0.85 },
  visible: { opacity: 1, scale: 1 }
};

const slideLeft = {
  hidden: { opacity: 0, x: -40 },
  visible: { opacity: 1, x: 0 }
};

const slideRight = {
  hidden: { opacity: 0, x: 40 },
  visible: { opacity: 1, x: 0 }
};

const zoomIn = {
  hidden: { opacity: 0, scale: 0.7 },
  visible: { opacity: 1, scale: 1 }
};

const staggerContainer = {
  hidden: {},
  visible: {
    transition: { staggerChildren: 0.1, delayChildren: 0.05 }
  }
};

const springTransition = { type: "spring", stiffness: 120, damping: 14 };

// ========== MAIN COMPONENT ==========
export default function AIAutomationPage() {
  const [openFaq, setOpenFaq] = useState(null);
  const [submitted, setSubmitted] = useState(false);
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState(null);
  const formRef = useRef(null);
  const successRef = useRef(null);

  const [formData, setFormData] = useState({
    name: "", email: "", company: "", service: "", details: ""
  });

  const { text: typedHeadline } = useTypewriter(ROTATING_WORDS);
  const { scrollY } = useScroll();
  const heroParallax = useTransform(scrollY, [0, 500], [0, -50]);

  const toggleFaq = (i) => setOpenFaq(openFaq === i ? null : i);

  const handleChange = (e) => {
    setFormData({ ...formData, [e.target.name]: e.target.value });
  };

  const handleSubmit = async (e) => {
    e.preventDefault();
    setLoading(true);
    setError(null);
    try {
      await apiService.submitLead({
        name: formData.name,
        email: formData.email,
        service: `AI Automation - ${formData.service || 'General'}`,
        message: `Company: ${formData.company}\nDetails: ${formData.details}`,
        company: formData.company,
        phone: ""
      });
      setSubmitted(true);
      setTimeout(() => {
        if (successRef.current) {
          successRef.current.scrollIntoView({ behavior: 'smooth', block: 'center' });
        }
      }, 100);
    } catch (err) {
      setError(err.message || "Failed to send. Please email info@scapedatasolutions.com");
    } finally {
      setLoading(false);
    }
  };

  // ===== DATA =====
  const stats = [
    { value: "85", label: "Efficiency Gain", suffix: "%" },
    { value: "60", label: "Cost Reduction", suffix: "%" },
    { value: "99.9", label: "System Uptime", suffix: "%" },
    { value: "4wks", label: "Weeks to Deploy", suffix: "" }
  ];

  const logos = [
    { name: "Vantage Robotics", link: "/case-studies" },
    { name: "NorthStar Analytics", link: "/case-studies" },
    { name: "Cascade Systems", link: "/case-studies" },
    { name: "Ionix Labs", link: "/case-studies" },
    { name: "Meridian AI", link: "/case-studies" }
  ];

  const comparison = [
    {
      process: "Invoice Processing",
      manual: "3–5 days turnaround, manual data entry, frequent errors.",
      automated: "Real-time processing, 99.9% accuracy, zero manual entry."
    },
    {
      process: "Customer Support",
      manual: "Limited to business hours, slow response times.",
      automated: "AI agents respond instantly, 24/7, with smart escalation."
    },
    {
      process: "Lead Qualification",
      manual: "Manual review, inconsistent scoring, days to follow up.",
      automated: "Autonomous scoring and instant, same-day follow-up."
    },
    {
      process: "Sales Forecasting",
      manual: "Spreadsheet guesswork, reactive planning.",
      automated: "Predictive analytics with 92% forecast accuracy."
    }
  ];

  const services = [
    {
      title: "Workflow Automation",
      desc: (
        <>
          <p>Deploy AI-powered automation that thinks, learns, and adapts. Transform manual workflows into intelligent processes that run themselves.</p>
          <p><Link to="/services/workflow-automation">Learn more →</Link></p>
        </>
      )
    },
    {
      title: "AI Agents",
      desc: (
        <>
          <p>Deploy autonomous AI agents that handle customer inquiries, process data, and execute complex business logic 24/7.</p>
          <p><Link to="/services/ai-agents">Explore AI Agents →</Link></p>
        </>
      )
    },
    {
      title: "Intelligent Analytics",
      desc: (
        <>
          <p>Gain real‑time insights with AI‑driven analytics that predict trends, detect anomalies, and prescribe actions.</p>
          <p><Link to="/services/intelligent-analytics">See Intelligent Analytics →</Link></p>
        </>
      )
    }
  ];

  const testimonial = {
    quote: '"Scape Data Solutions has revolutionised our operations. We\'ve cut processing times by 70% and our team can now focus on strategic initiatives."',
    name: "Sarah Chen",
    role: "VP of Operations, NexGen Logistics"
  };

  const resources = [
    {
      badge: "Case Study",
      title: "How Acme Corp automated their supply chain with Scape AI",
      link: "/case-studies/acme-corp",
      linkText: "Read the story"
    },
    {
      badge: "Guide",
      title: "The Ultimate Guide to AI Workflow Automation",
      link: "/resources/ai-workflow-guide",
      linkText: "Download now"
    },
    {
      badge: "Webinar",
      title: "On‑Demand: Building Your Automation Roadmap",
      link: "/resources/automation-roadmap-webinar",
      linkText: "Watch now"
    },
    {
      badge: "Whitepaper",
      title: "The ROI of AI Automation: A Data‑Driven Approach",
      link: "/resources/ai-roi-whitepaper",
      linkText: "Get the whitepaper"
    }
  ];

  const faqs = [
    {
      q: "What types of workflows can you automate?",
      a: "We automate data entry, document processing, customer support, lead qualification, invoice handling, and IT ticketing – integrating with 200+ apps."
    },
    {
      q: "How secure is your AI platform?",
      a: "We are SOC 2 Type II compliant, use enterprise‑grade encryption, and never use your data to train models for other customers."
    },
    {
      q: "Can I use my own AI models with Scape Data Solutions?",
      a: "Absolutely. We offer a 'Bring Your Own Model' capability, giving you full control while leveraging our orchestration and security layers."
    },
    {
      q: "How long does it take to deploy an automation?",
      a: "Most workflows are deployed within 2–4 weeks, depending on complexity. We work closely with your team throughout."
    },
    {
      q: "What support do you offer during and after deployment?",
      a: "We provide a dedicated solution architect, 24/7 monitoring, ongoing model retraining, and regular business reviews."
    }
  ];

  const features = [
    { icon: <Bot size={22} />, title: "AI‑powered Automation", desc: "Intelligent agents that learn and adapt to your business processes." },
    { icon: <Workflow size={22} />, title: "End‑to‑End Orchestration", desc: "Connect people, data, and systems in a single automated flow." },
    { icon: <Brain size={22} />, title: "Cognitive Decision‑Making", desc: "Advanced reasoning that makes autonomous decisions based on context." },
    { icon: <BarChart3 size={22} />, title: "Real‑time Analytics", desc: "Monitor performance and gain insights with live dashboards." },
    { icon: <Shield size={22} />, title: "Enterprise‑Grade Security", desc: "SOC 2, GDPR, and data‑residency compliant." },
    { icon: <GitMerge size={22} />, title: "Continuous Improvement", desc: "Models retrain and improve automatically over time." }
  ];

  const processSteps = [
    { num: "01", title: "Discover & Define", desc: "We analyse your current workflows and identify automation opportunities." },
    { num: "02", title: "Design & Prototype", desc: "We design a solution and build a prototype for validation." },
    { num: "03", title: "Deploy & Integrate", desc: "We deploy the automation and integrate it with your existing systems." },
    { num: "04", title: "Optimise & Scale", desc: "We monitor performance, retrain models, and scale as you grow." }
  ];

  return (
    <div className={styles.page}>
      <SEO
        title="AI Automation Services | Workflow Automation & AI Agents"
        description="Deploy intelligent automation that thinks, learns, and adapts. Transform manual workflows with AI agents, predictive analytics, and autonomous decision-making."
        path="/services/ai-automation"
      />

      <Navbar activeNav="services" />

      <motion.main
        initial={{ opacity: 0 }}
        animate={{ opacity: 1 }}
        transition={{ duration: 1, ease: "easeOut" }}
      >

        {/* ========== HERO – NOW ANIMATES ON SCROLL ========== */}
        <motion.section
          className={styles.hero}
          style={{ y: heroParallax }}
        >
          <div className={styles.container}>
            <div className={styles.heroInner}>

              <motion.div
                className={styles.heroContent}
                variants={staggerContainer}
                initial="hidden"
                whileInView="visible"
                viewport={{ amount: 0.2, once: false }}
              >
                <motion.div
                  className={styles.heroBadge}
                  variants={fadeUp}
                  transition={{ duration: 0.6 }}
                >
                </motion.div>

                <motion.h1 variants={fadeUp} transition={{ duration: 0.7 }}>
                  Automate Your Intelligent Future
                </motion.h1>

                <motion.div
                  className={styles.heroTypewriter}
                  variants={fadeUp}
                  transition={{ duration: 0.7 }}
                >
                  <h2>{typedHeadline}<span className={styles.caret}>|</span></h2>
                </motion.div>

                <motion.p
                  className={styles.heroDesc}
                  variants={fadeUp}
                  transition={{ duration: 0.7 }}
                >
                  Deploy AI‑powered automation that thinks, learns, and adapts. Transform manual workflows into intelligent processes that run themselves.
                </motion.p>

                <motion.div
                  className={styles.heroStats}
                  variants={staggerContainer}
                >
                  {[
                    { value: "15,000+", label: "Workflows Automated" },
                    { value: "99.9%", label: "Uptime" },
                    { value: "4wks", label: "Avg. Deployment" }
                  ].map((stat, i) => (
                    <motion.div
                      key={i}
                      className={styles.heroStat}
                      variants={scaleIn}
                      transition={{ ...springTransition, delay: i * 0.1 }}
                    >
                      <span className={styles.heroStatValue}>
                        <AnimatedStat value={stat.value.replace(/[^0-9.]/g, "")} suffix={stat.value.replace(/[0-9.]/g, "")} />
                      </span>
                      <span className={styles.heroStatLabel}>{stat.label}</span>
                    </motion.div>
                  ))}
                </motion.div>

                <motion.div
                  className={styles.heroButtons}
                  variants={fadeUp}
                  transition={{ duration: 0.7 }}
                >
                  <motion.a
                    href="#contact"
                    className={styles.btnPrimary}
                    whileHover={{ scale: 1.05 }}
                    whileTap={{ scale: 0.97 }}
                  >
                    Start Your Automation
                    <ArrowRight size={18} />
                  </motion.a>
                  <motion.a
                    href="#features"
                    className={styles.btnSecondary}
                    whileHover={{ scale: 1.05 }}
                    whileTap={{ scale: 0.97 }}
                  >
                    Explore Features
                  </motion.a>
                </motion.div>
              </motion.div>

              <motion.div
                className={styles.heroVisual}
                initial={{ opacity: 0, scale: 0.9, y: 20 }}
                whileInView={{ opacity: 1, scale: 1, y: 0 }}
                viewport={{ amount: 0.2, once: false }}
                transition={{ duration: 0.9, delay: 0.2 }}
              >
                <div className={styles.automationDemo}>
                  <div className={styles.demoHeader}>
                    <span className={styles.demoBadge}>AI Automation</span>
                    <div className={styles.demoDots}>
                      <span className={styles.demoDot} />
                      <span className={styles.demoDot} />
                      <span className={styles.demoDot} />
                    </div>
                  </div>
                  <div className={styles.demoBody}>
                    <div className={styles.demoLine}>
                      <span className={styles.demoLabel}>Task</span>
                      <span className={styles.demoText}>Invoice processing</span>
                      <span className={styles.demoCorrection}>✅ Automated</span>
                    </div>
                    <div className={styles.demoLine}>
                      <span className={styles.demoLabel}>Agent</span>
                      <span className={styles.demoText}>Customer support</span>
                      <span className={styles.demoCorrection}>🤖 24/7</span>
                    </div>
                    <div className={styles.demoLine}>
                      <span className={styles.demoLabel}>Analytics</span>
                      <span className={styles.demoText}>Sales forecasting</span>
                      <span className={styles.demoCorrection}>📈 92% accuracy</span>
                    </div>
                    <div className={styles.demoLine}>
                      <span className={styles.demoLabel}>Decision</span>
                      <span className={styles.demoText}>Lead qualification</span>
                      <span className={styles.demoCorrection}>⚡ Autonomous</span>
                    </div>
                    <motion.div
                      className={styles.demoCursor}
                      animate={{ x: [0, 280, 0] }}
                      transition={{ duration: 1.6, repeat: Infinity, ease: "easeInOut" }}
                    />
                  </div>
                  <div className={styles.demoFooter}>
                    <span className={styles.demoStats}>🤖 4 agents active</span>
                    <span className={styles.demoStats}>⚡ 1.2k tasks today</span>
                    <span className={styles.demoStats}>📊 99.7% accuracy</span>
                  </div>
                </div>
              </motion.div>
            </div>
          </div>
        </motion.section>

        {/* ========== STATS ========== */}
        <motion.section
          className={styles.statsSection}
          initial="hidden"
          whileInView="visible"
          viewport={{ amount: 0.2 }}
          variants={staggerContainer}
        >
          <div className={styles.container}>
            <motion.div className={styles.statsGrid} variants={staggerContainer}>
              {stats.map((stat, i) => (
                <motion.div
                  key={i}
                  className={styles.statCard}
                  variants={scaleIn}
                  transition={{ ...springTransition, delay: i * 0.08 }}
                >
                  <div className={styles.statValue}>
                    <AnimatedStat value={stat.value} suffix={stat.suffix} />
                  </div>
                  <div className={styles.statLabel}>{stat.label}</div>
                </motion.div>
              ))}
            </motion.div>
          </div>
        </motion.section>

        {/* ========== LOGO GRID ========== */}
        <motion.section
          className={styles.logoSection}
          initial="hidden"
          whileInView="visible"
          viewport={{ amount: 0.1 }}
          variants={staggerContainer}
        >
          <div className={styles.container}>
            <motion.div
              className={styles.logoHeader}
              variants={fadeUp}
              transition={{ duration: 0.6 }}
            >
              <h2>Trusted by innovative companies worldwide</h2>
            </motion.div>
            <motion.div className={styles.logoGrid} variants={staggerContainer}>
              {logos.map((logo, i) => (
                <motion.div
                  key={i}
                  className={styles.logoItem}
                  variants={scaleIn}
                  transition={{ ...springTransition, delay: i * 0.05 }}
                >
                  <Link to={logo.link} className={styles.logoText}>
                    {logo.name}
                  </Link>
                </motion.div>
              ))}
            </motion.div>
          </div>
        </motion.section>

        {/* ========== COMPARISON ========== */}
        <motion.section
          className={styles.timelineSection}
          initial="hidden"
          whileInView="visible"
          viewport={{ amount: 0.15 }}
          variants={staggerContainer}
        >
          <div className={styles.container}>
            <motion.div
              className={styles.timelineHeader}
              variants={fadeUp}
              transition={{ duration: 0.6 }}
            >
              <h2>See the difference automation makes</h2>
              <p>How manual processes transform once they run on Scape AI.</p>
            </motion.div>
            <motion.div className={styles.timelineTable} variants={fadeUp}>
              <table>
                <thead>
                  <tr>
                    <th>Process</th>
                    <th>Manual Approach</th>
                    <th>With Scape AI</th>
                  </tr>
                </thead>
                <tbody>
                  {comparison.map((row, i) => (
                    <tr key={i}>
                      <td>{row.process}</td>
                      <td>{row.manual}</td>
                      <td className={styles.automatedCell}>{row.automated}</td>
                    </tr>
                  ))}
                </tbody>
              </table>
            </motion.div>
          </div>
        </motion.section>

        {/* ========== PROMO ========== */}
        <motion.section
          className={styles.promoSection}
          initial="hidden"
          whileInView="visible"
          viewport={{ amount: 0.1 }}
          variants={staggerContainer}
        >
          <div className={styles.container}>
            <motion.div
              className={styles.promoInner}
              variants={zoomIn}
              transition={springTransition}
            >
              <div className={styles.promoContent}>
                <h2>Ready to see the difference?</h2>
                <p>Get started with a free 14‑day trial of our AI automation platform. No credit card required.</p>
                <div className={styles.promoButtons}>
                  <Link to="/contact" className={styles.btnPrimary}>Start free trial</Link>
                  <Link to="/contact" className={styles.btnSecondary}>Schedule a demo</Link>
                </div>
              </div>
            </motion.div>
          </div>
        </motion.section>

        {/* ========== SERVICES ========== */}
        <motion.section
          className={styles.servicesSection}
          initial="hidden"
          whileInView="visible"
          viewport={{ amount: 0.1 }}
          variants={staggerContainer}
        >
          <div className={styles.container}>
            <motion.div
              className={styles.servicesHeader}
              variants={fadeUp}
              transition={{ duration: 0.6 }}
            >
              <h2>Our AI‑Powered Services</h2>
              <p>End‑to‑end automation solutions that drive efficiency, reduce costs, and enable innovation.</p>
            </motion.div>
            <motion.div className={styles.servicesGrid} variants={staggerContainer}>
              {services.map((service, i) => (
                <motion.div
                  key={i}
                  className={styles.serviceCard}
                  variants={scaleIn}
                  transition={{ ...springTransition, delay: i * 0.08 }}
                  whileHover={{ y: -6 }}
                >
                  <div className={styles.serviceContent}>
                    <h3>{service.title}</h3>
                    {service.desc}
                  </div>
                </motion.div>
              ))}
            </motion.div>
          </div>
        </motion.section>

        {/* ========== FEATURES ========== */}
        <motion.section
          id="features"
          className={styles.featuresSection}
          initial="hidden"
          whileInView="visible"
          viewport={{ amount: 0.1 }}
          variants={staggerContainer}
        >
          <div className={styles.container}>
            <motion.div
              className={styles.featuresHeader}
              variants={fadeUp}
              transition={{ duration: 0.6 }}
            >
              <h2>Key Capabilities</h2>
              <p>Everything you need to automate and optimise your business.</p>
            </motion.div>
            <motion.div className={styles.featuresGrid} variants={staggerContainer}>
              {features.map((feature, i) => (
                <motion.div
                  key={i}
                  className={styles.featureCard}
                  variants={fadeUp}
                  transition={{ duration: 0.5, delay: i * 0.05 }}
                  whileHover={{ y: -6, scale: 1.02 }}
                >
                  <div className={styles.featureIcon}>{feature.icon}</div>
                  <h3>{feature.title}</h3>
                  <p>{feature.desc}</p>
                </motion.div>
              ))}
            </motion.div>
          </div>
        </motion.section>

        {/* ========== TESTIMONIAL ========== */}
        <motion.section
          className={styles.testimonialSection}
          initial="hidden"
          whileInView="visible"
          viewport={{ amount: 0.15 }}
          variants={staggerContainer}
        >
          <div className={styles.container}>
            <motion.div
              className={styles.testimonialCard}
              variants={scaleIn}
              transition={springTransition}
              whileHover={{ scale: 1.01 }}
            >
              <div className={styles.testimonialContent}>
                <span className={styles.quoteMark}>“</span>
                <p>{testimonial.quote}</p>
                <div className={styles.testimonialAuthor}>
                  <div>
                    <strong>{testimonial.name}</strong>
                    <span>{testimonial.role}</span>
                  </div>
                </div>
              </div>
            </motion.div>
          </div>
        </motion.section>

        {/* ========== PROCESS ========== */}
        <motion.section
          className={styles.processSection}
          initial="hidden"
          whileInView="visible"
          viewport={{ amount: 0.1 }}
          variants={staggerContainer}
        >
          <div className={styles.container}>
            <motion.div
              className={styles.processHeader}
              variants={fadeUp}
              transition={{ duration: 0.6 }}
            >
              <h2>How We Automate</h2>
              <p>Four steps to transform your workflows.</p>
            </motion.div>
            <motion.div className={styles.processSteps} variants={staggerContainer}>
              {processSteps.map((step, i) => (
                <motion.div
                  key={i}
                  className={styles.processStep}
                  variants={i % 2 === 0 ? slideLeft : slideRight}
                  transition={{ duration: 0.5, delay: i * 0.12 }}
                >
                  <motion.div
                    className={styles.processNum}
                    initial={{ scale: 0 }}
                    whileInView={{ scale: 1 }}
                    viewport={{ once: false, amount: 0.3 }}
                    transition={{ ...springTransition, delay: i * 0.12 + 0.15 }}
                  >
                    {step.num}
                  </motion.div>
                  <h3>{step.title}</h3>
                  <p>{step.desc}</p>
                </motion.div>
              ))}
            </motion.div>
          </div>
        </motion.section>

        {/* ========== RESOURCES ========== */}
        <motion.section
          className={styles.resourcesSection}
          initial="hidden"
          whileInView="visible"
          viewport={{ amount: 0.1 }}
          variants={staggerContainer}
        >
          <div className={styles.container}>
            <motion.div
              className={styles.resourcesHeader}
              variants={fadeUp}
              transition={{ duration: 0.6 }}
            >
              <h2>Resources to accelerate your automation journey</h2>
            </motion.div>
            <motion.div className={styles.resourcesGrid} variants={staggerContainer}>
              {resources.map((res, i) => (
                <motion.div
                  key={i}
                  className={styles.resourceCard}
                  variants={scaleIn}
                  transition={{ ...springTransition, delay: i * 0.06 }}
                  whileHover={{ y: -6 }}
                >
                  <div className={styles.resourceContent}>
                    <span className={styles.resourceBadge}>{res.badge}</span>
                    <h3><Link to={res.link}>{res.title}</Link></h3>
                    <Link to={res.link} className={styles.resourceLink}>{res.linkText}</Link>
                  </div>
                </motion.div>
              ))}
            </motion.div>
            <div className={styles.resourcesCta}>
              <Link to="/resources" className={styles.btnSecondary}>View all resources</Link>
            </div>
          </div>
        </motion.section>

        {/* ========== CONTACT ========== */}
        <motion.section
          id="contact"
          className={styles.ctaSection}
          initial="hidden"
          whileInView="visible"
          viewport={{ amount: 0.1 }}
          variants={fadeUp}
          transition={{ duration: 0.6 }}
          ref={formRef}
        >
          <div className={styles.container}>
            <motion.div
              className={styles.ctaBox}
              variants={scaleIn}
              transition={springTransition}
            >
              <AnimatePresence mode="wait">
                {submitted ? (
                  <motion.div
                    key="success"
                    ref={successRef}
                    className={styles.successMessage}
                    initial={{ opacity: 0, scale: 0.7 }}
                    animate={{ opacity: 1, scale: 1 }}
                    transition={springTransition}
                  >
                    <CheckCircle size={48} />
                    <h3>Thank You!</h3>
                    <p>We've received your request. A specialist will contact you within 24 hours.</p>
                  </motion.div>
                ) : (
                  <motion.div
                    key="form"
                    initial={{ opacity: 0 }}
                    animate={{ opacity: 1 }}
                    exit={{ opacity: 0 }}
                  >
                    <motion.h2 variants={fadeUp} initial="hidden" animate="visible">
                      Let's talk about your automation needs
                    </motion.h2>
                    <motion.p variants={fadeUp} initial="hidden" animate="visible" transition={{ delay: 0.05 }}>
                      Our team will help you design the perfect automation strategy.
                    </motion.p>

                    {error && (
                      <motion.div
                        className={styles.errorMessage}
                        initial={{ opacity: 0, height: 0 }}
                        animate={{ opacity: 1, height: 'auto' }}
                        exit={{ opacity: 0, height: 0 }}
                      >
                        <p>{error}</p>
                      </motion.div>
                    )}

                    <motion.form
                      onSubmit={handleSubmit}
                      className={styles.contactForm}
                      variants={staggerContainer}
                      initial="hidden"
                      animate="visible"
                    >
                      <motion.div className={styles.formRow} variants={fadeUp}>
                        <div className={styles.formGroup}>
                          <input
                            type="text"
                            name="name"
                            placeholder="Full Name *"
                            required
                            value={formData.name}
                            onChange={handleChange}
                          />
                        </div>
                        <div className={styles.formGroup}>
                          <input
                            type="email"
                            name="email"
                            placeholder="Email Address *"
                            required
                            value={formData.email}
                            onChange={handleChange}
                          />
                        </div>
                      </motion.div>

                      <motion.div className={styles.formRow} variants={fadeUp}>
                        <div className={styles.formGroup}>
                          <input
                            type="text"
                            name="company"
                            placeholder="Company Name"
                            value={formData.company}
                            onChange={handleChange}
                          />
                        </div>
                        <div className={styles.formGroup}>
                          <select
                            name="service"
                            required
                            value={formData.service}
                            onChange={handleChange}
                          >
                            <option value="">Service of Interest *</option>
                            <option value="workflow">Workflow Automation</option>
                            <option value="agents">AI Agents</option>
                            <option value="analytics">Intelligent Analytics</option>
                            <option value="consulting">AI Consulting</option>
                            <option value="custom">Custom Solution</option>
                          </select>
                        </div>
                      </motion.div>

                      <motion.div className={styles.formGroup} variants={fadeUp}>
                        <textarea
                          name="details"
                          placeholder="Tell us about your business challenges and what you'd like to automate..."
                          rows={5}
                          required
                          value={formData.details}
                          onChange={handleChange}
                        />
                      </motion.div>

                      <motion.button
                        type="submit"
                        className={styles.submitButton}
                        disabled={loading}
                        variants={fadeUp}
                        whileHover={{ scale: 1.02 }}
                        whileTap={{ scale: 0.97 }}
                      >
                        {loading ? (
                          <><Loader2 className={styles.spinner} size={18} /> Processing...</>
                        ) : (
                          <>Submit Request <Send size={16} /></>
                        )}
                      </motion.button>

                      <motion.p className={styles.privacyNote} variants={fadeUp}>
                        We respect your privacy. Your data stays secure and confidential.
                      </motion.p>
                    </motion.form>
                  </motion.div>
                )}
              </AnimatePresence>
            </motion.div>
          </div>
        </motion.section>

        {/* ========== FAQ ========== */}
        <motion.section
          className={styles.faqSection}
          initial="hidden"
          whileInView="visible"
          viewport={{ amount: 0.1 }}
          variants={staggerContainer}
        >
          <div className={styles.container}>
            <motion.div
              className={styles.faqHeader}
              variants={fadeUp}
              transition={{ duration: 0.6 }}
            >
              <h2>Frequently Asked Questions</h2>
            </motion.div>
            <motion.div className={styles.faqGrid} variants={staggerContainer}>
              {faqs.map((faq, i) => (
                <motion.div
                  key={i}
                  className={styles.faqItem}
                  variants={fadeUp}
                  transition={{ duration: 0.45, delay: i * 0.04 }}
                >
                  <button
                    className={styles.faqQuestion}
                    onClick={() => toggleFaq(i)}
                  >
                    <span>{faq.q}</span>
                    <motion.span
                      animate={{ rotate: openFaq === i ? 180 : 0 }}
                      transition={{ duration: 0.3 }}
                    >
                      <ChevronDown size={18} />
                    </motion.span>
                  </button>
                  <AnimatePresence>
                    {openFaq === i && (
                      <motion.div
                        initial={{ height: 0, opacity: 0 }}
                        animate={{ height: 'auto', opacity: 1 }}
                        exit={{ height: 0, opacity: 0 }}
                        className={styles.faqAnswer}
                      >
                        {faq.a}
                      </motion.div>
                    )}
                  </AnimatePresence>
                </motion.div>
              ))}
            </motion.div>
          </div>
        </motion.section>

      </motion.main>

      <Footer />
    </div>
  );
}