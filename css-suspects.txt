=== Portfolio 'force overflow visible' block (lines 1010-1055) ===
  }
}

/* ──────────────────────────────────────────────────────────────
   FIX: Ensure dropdowns are never clipped
   ────────────────────────────────────────────────────────────── */

/* Force all ancestors to allow overflow */
.page {
  overflow: visible !important;
}

/* The main content container must also be visible */
.mainContent {
  overflow: visible !important;
}

/* The navbar itself should not clip its absolute children */
.navbarFixed {
  overflow: visible !important;
}

/* Dropdown containers need highest z‑index and no clipping */
.drop,
.dropWide,
.dropGrid,
.dropGrid1 {
  z-index: 9999 !important;
  overflow: visible !important;
}

/* Every list item that triggers a dropdown must allow its child to overflow */
.hasDrop {
  overflow: visible !important;
  position: relative;
}

/* Additional safety for any wrapper that might clip */
.container,
.navbarInner,
.navList {
  overflow: visible !important;
}
=== Full App.css ===
     1	#root {
     2	    max-width: 1280px;
     3	    margin: 0 auto;
     4	    padding: 2rem;
     5	    text-align: center;
     6	}
     7	
     8	.logo {
     9	    height: 6em;
    10	    padding: 1.5em;
    11	    will-change: filter;
    12	    transition: filter 300ms;
    13	}
    14	
    15	.logo:hover {
    16	    filter: drop-shadow(0 0 2em #646cffaa);
    17	}
    18	
    19	.logo.react:hover {
    20	    filter: drop-shadow(0 0 2em #61dafbaa);
    21	}
    22	
    23	@keyframes logo-spin {
    24	    from {
    25	        transform: rotate(0deg);
    26	    }
    27	
    28	    to {
    29	        transform: rotate(360deg);
    30	    }
    31	}
    32	
    33	@media (prefers-reduced-motion: no-preference) {
    34	    a:nth-of-type(2) .logo {
    35	        animation: logo-spin infinite 20s linear;
    36	    }
    37	}
    38	
    39	.card {
    40	    padding: 2em;
    41	}
    42	
    43	.read-the-docs {
    44	    color: #888;
    45	}
=== Full index.css ===
     1	@tailwind base;
     2	@tailwind components;
     3	@tailwind utilities;
     4	
     5	html, body {
     6	    margin: 0;
     7	    max-width: 100vw;
     8	    overflow-x: hidden;
     9	    font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', 'Roboto', 'Oxygen',
    10	        'Ubuntu', 'Cantarell', 'Fira Sans', 'Droid Sans', 'Helvetica Neue',
    11	        sans-serif;
    12	    -webkit-font-smoothing: antialiased;
    13	    -moz-osx-font-smoothing: grayscale;
    14	}
    15	
    16	#root {
    17	  overflow-x: hidden;
    18	}
